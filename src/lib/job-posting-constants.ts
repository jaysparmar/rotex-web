export const EMPLOYMENT_TYPES = ["Full-time", "Part-time", "Contract", "Internship"];
export const WORK_MODES = ["On-site", "Hybrid", "Remote"];

export const EMPLOYMENT_TYPE_OPTIONS = EMPLOYMENT_TYPES.map((v) => ({ value: v, label: v }));
export const WORK_MODE_OPTIONS = WORK_MODES.map((v) => ({ value: v, label: v }));
